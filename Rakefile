require 'musicality'

include Musicality
include Pitches
include Meters
include Keys
include Articulations
include Dynamics

config = Project.load_config(File.dirname(__FILE__))
Project.create_tasks(config)
